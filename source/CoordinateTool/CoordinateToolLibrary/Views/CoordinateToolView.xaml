<UserControl x:Class="CoordinateToolLibrary.Views.CoordinateToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:CoordinateToolLibrary.ViewModels"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <viewModels:CoordinateToolViewModel x:Key="CTViewModel" />
    </UserControl.Resources>
    <Grid DataContext="{StaticResource CTViewModel}">
        <TextBlock Text="{Binding CoordinateToolTitle}" />
    </Grid>
</UserControl>
